'''ServerRoot "/usr/local/apache2"
Listen 80

# Modules de base nécessaires
LoadModule mpm_event_module modules/mod_mpm_event.so
LoadModule authn_core_module modules/mod_authn_core.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule unixd_module modules/mod_unixd.so
LoadModule dir_module modules/mod_dir.so
LoadModule mime_module modules/mod_mime.so
LoadModule log_config_module modules/mod_log_config.so
LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_http_module modules/mod_proxy_http.so

# MODULES REVERSE PROXY (Crucial pour la redirection)
LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_http_module modules/mod_proxy_http.so

ServerName localhost

<VirtualHost *:80>
    ProxyPreserveHost On
    # On redirige tout le trafic du port 80 vers le conteneur 'backend' port 8080
    ProxyPass / http://backend:8080/
    ProxyPassReverse / http://backend:8080/
    
</VirtualHost>

# Configuration des logs pour les voir dans Docker
ErrorLog /proc/self/fd/2
CustomLog /proc/self/fd/1 common'''

ServerRoot "/usr/local/apache2"
Listen 80

# Modules de base
LoadModule mpm_event_module modules/mod_mpm_event.so
LoadModule authn_core_module modules/mod_authn_core.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule unixd_module modules/mod_unixd.so
LoadModule dir_module modules/mod_dir.so
LoadModule mime_module modules/mod_mime.so
LoadModule log_config_module modules/mod_log_config.so

# Modules Proxy (chargés une seule fois)
LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_http_module modules/mod_proxy_http.so

ServerName localhost

<VirtualHost *:80>
    ProxyPreserveHost On

    # 1. Redirection vers l'API (Backend)
    # Important : on met l'API en premier !
    ProxyPass "/api" "http://my-app:8080/"
    ProxyPassReverse "/api" "http://my-app:8080/"

    # 2. Redirection vers le Front-end
    # Le "/" capture tout ce qui ne commence pas par /api
    ProxyPass "/" "http://my-front:80/"
    ProxyPassReverse "/" "http://my-front:80/"
</VirtualHost>

# Logs
ErrorLog /proc/self/fd/2
CustomLog /proc/self/fd/1 common