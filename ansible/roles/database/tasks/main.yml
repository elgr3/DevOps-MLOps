- name: Run Database
  docker_container:
    name: postgres-container
    image: rodyb/tp-devops-database:latest
    networks:
      - name: app-network
    env:
      POSTGRES_DB: db
      POSTGRES_USER: usr
      POSTGRES_PASSWORD: pwd
    restart_policy: always