- name: Run Backend App
  docker_container:
    name: backend
    image: rodyb/tp-devops-backend:latest
    networks:
      - name: app-network
    env:
      POSTGRES_DB: db
      POSTGRES_USER: usr
      POSTGRES_PASSWORD: pwd
    restart_policy: always

