- name: Run Proxy
  docker_container:
    name: my-proxy
    image: rodyb/tp-devops-httpd:latest
    pull: true
    networks:
      - name: app-network
    ports:
      - "80:80"
    restart_policy: always